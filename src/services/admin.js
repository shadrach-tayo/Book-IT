function createAdminService({}) {
  // can add hotels,
  // can edit hotels,
  // can delete hotels,
  // can suspend hotels,

  // add user
  // edit user info
  // delete user acct

  return {};
}

module.exports = createAdminService;
